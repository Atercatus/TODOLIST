extends layouts/main
include mixins/listBlock

block content
        .list-container
            each list in todolists.reverse()
                +listBlock({
                    id: list.id,
                    title: list.listTitle
                })