extends layouts/main

block content
    .form-container
        if errmsg
            .form__errmsg=errmsg
        form(action=`${routes.newlist}`, method="post")
            input(type="text", name="title", placeholder="Title", required=true)
            input(type="submit", value="Add Now")