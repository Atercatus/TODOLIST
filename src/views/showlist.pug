extends layouts/main
include mixins/listBlock

block content
        .list-container
            if todolists
                each list in todolists
                    +listBlock({
                        id: list.id,
                        title: list.listTitle,
                        remainders: list.remainingTask,
                        cautions: list.cautions
                    })